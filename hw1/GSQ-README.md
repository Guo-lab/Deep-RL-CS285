# Q1:
- The folder q1 includes the analysis of the first question.
- `data` folder's subfolders start with the prefix `q1_`.
- `run_batch_experiments.sh`.
- `analyze_results.py`.
- `q1_tables.tex` and `q1_tables.pdf`.

# Q2:
- `data` folder's subfolders start with the prefix `q2_`.
- `run_dagger_experiments.sh`.
- `analyze_dagger_results.py`.
- `plots` folder.
- `q2_report.tex` and `q2_report.pdf`.